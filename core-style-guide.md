# Core Style Guide · Prompt Blueprints

이 문서는 `blueprints/` 아래 **모든 블루프린트**, 그리고 블루프린트가 **생성하는 모든 레포·문서**가 공유하는 단일 규약입니다.
새 블루프린트를 쓰거나 기존 것을 고칠 때, 그리고 블루프린트로 레포를 생성하는 에이전트가 따라야 할 기준이 여기에 있습니다.

> 한 줄 요약: **사용법이 아니라 본질을, 설명이 아니라 증거를.**

---

## 0. 블루프린트란 무엇인가

블루프린트는 읽는 글이 아니라 **실행되는 지시문(meta-prompt)** 입니다.
하나의 `.md`를 코딩 에이전트에 그대로 건네면, 에이전트는 그것을 읽고 **딥다이브 학습 레포 한 채**(수십 개 문서 + README + 실험 환경)를 처음부터 끝까지 작성합니다.

따라서 블루프린트는 두 종류의 규약을 동시에 만족해야 합니다.

1. **블루프린트 자체의 규약** — 이 파일이 어떤 골격·톤·정보를 담아야 하는가 (§2~§4)
2. **생성물의 규약** — 그 지시로 만들어질 레포·문서가 어떤 구조·증거 기준을 따라야 하는가 (§5~§7)

---

## 1. 다섯 가지 황금 규칙

모든 블루프린트와 생성물이 예외 없이 지키는 원칙입니다.

### 1.1 사용 ≠ 본질 (Use vs Essence)
블루프린트의 **첫 문장**은 반드시 *"…하는 것과 …아는 것은 다르다"* 패턴으로 **사용**과 **본질** 사이의 간극을 못 박습니다.

> `np.linalg.svd`를 **호출하는 것**과, 모든 행렬이 왜 회전·스케일·회전으로 분해되는지 **증명할 수 있는 것**은 다르다.
> `synchronized`를 **쓰는 것**과, JVM이 삽입하는 Memory Barrier를 **어셈블리로 확인하는 것**은 다르다.

이 한 문장이 레포의 존재 이유이자 난이도 계약입니다.

### 1.2 왜(Why) 중심
"무엇인가"가 아니라 **"왜 이렇게 설계됐는가"** 를 질문의 축으로 삼습니다.
메커니즘을 설명한 뒤에는 항상 **설계 의도·트레이드오프**로 한 발 더 들어갑니다. ("이렇게 동작한다" → "왜 이렇게 설계됐는가")

### 1.3 증거주의 (Evidence over Prose)
**글로 설명하지 않습니다.** 모든 주장은 셋 중 하나로 증명합니다.

| 영역 | 증거의 형태 |
|------|-------------|
| 시스템/Dev | 도구 출력 — `javap`, `JMH`, `JFR`, `EXPLAIN ANALYZE`, `strace`, `tcpdump`, `-XX:+PrintCompilation` … |
| 수학/AI | 정리와 증명 + NumPy/PyTorch로 수치 검증 (이론과 코드 결과가 일치함을 보임) |
| 공통 | 실행 가능한 최소 재현 코드 + 그 출력 |

"빠르다", "안전하다", "더 효율적이다" 같은 형용사는 **반드시 수치·출력으로 뒷받침**합니다.

### 1.4 블랙박스 분해
통념 한 줄("GC는 메모리를 수거합니다")을 **알고리즘 단계까지** 풀어 헤칩니다.
필요하면 하드웨어(캐시 라인·메모리 배리어·SRAM/HBM)나 1차 소스코드(HotSpot, V8, Lucene…)까지 내려갑니다.

### 1.5 레포는 연결된다
모든 레포는 고립되지 않습니다. 블루프린트는 **선행 학습 / 레포 연결**을 명시합니다.

- **AI:** Layer 0 → 6 수직 커리큘럼. 상위 레이어는 하위 레이어를 *필수/권장 선행*으로 링크. (예: Transformer ← NN Theory, Linear Algebra, Kernel Methods)
- **Dev:** 가로 의존. (예: Spring Data ← JVM, Database Internals) 그리고 `synthesis/`가 여러 레포를 입력으로 묶는 수렴점.

---

## 2. 블루프린트 파일 골격

블루프린트는 아래 골격을 따릅니다. 섹션 제목은 **이모지 + 한국어**로 통일합니다.

```markdown
# {주제} Deep Dive 레포지토리 제작 프롬프트

나는 "{주제} Deep Dive" 레포지토리를 만들려고 해.
{사용 ≠ 본질 첫 문장 — §1.1}
이 모든 "다름"을 완전히 파헤치는 레포를 만들 거야.

## 📋 프로젝트 목표
**컨셉**: "{한 문장으로 압축한 본질}"
**핵심 차별화**: 1~5개 항목 (이 레포가 일반 자료와 다른 점)
**타겟 독자**: 구체적 페르소나 (면접 준비생 / 튜닝 실무자 / …)
**선행 학습 / 레포 연결**: 필수·권장 선행 레포 링크 (§1.5)

## 🎯 1단계: 전체 구조  ← 확정형이면 "확정 — 변경 금지", 설계형이면 "먼저 설계"
섹션/챕터 → 문서 표(문서 | 다루는 내용), 핵심 질문, 총 문서 수 명시

## 📐 문서 구조
생성될 각 문서가 따를 10섹션 표준 (§5) — 코드블록으로 명시

## 🎨 스타일 가이드
증거 규칙·기준 버전·분량 등 이 레포 고유의 규칙 (§1, §7 구체화)

## 🛠️ 실험·검증 환경
도구 목록 / requirements.txt / Dockerfile 등 (§6)

## 🎯 2단계: 작업 순서
디렉토리 생성 → README → 섹션별 문서 (순서·분량 명시)

## 📚 참고 자료
1차 출처 — 공식 스펙·논문·소스코드 (§7.3)

## 💡 핵심 분석 대상
이 레포의 하이라이트가 될 대표 증명/분해 (코드·다이어그램 예시)

## 🎯 진행 방식
에이전트에게 주는 시작 신호 ("1단계부터" 또는 "디렉토리부터")
```

각 섹션의 필수 요소:

- **프로젝트 목표** — `컨셉`·`핵심 차별화`·`타겟 독자`는 빠질 수 없습니다. 타겟 독자는 "주니어/시니어"가 아니라 *"`volatile`이 정확히 무엇을 보장하는지 답하고 싶은 면접 준비생"* 처럼 구체적이어야 합니다.
- **전체 구조** — 각 섹션에 `> **핵심 질문:**` 한 줄을 답니다. 문서는 `| 문서 | 다루는 내용 |` 표로 나열하고, **총 문서 수를 산식으로 확정**합니다. (`7+7+11+… = 69개 (확정)`)
- **핵심 분석 대상** — 이 레포가 무엇을 "증명"하는지 미리 보여주는 쇼케이스. 도구 출력·어셈블리·수식 분해를 코드블록으로 담습니다.

---

## 3. 세 가지 변형

| 변형 | 1단계 처리 | 진행 방식 신호 | 비고 |
|------|------------|----------------|------|
| **구조 확정형** | 챕터·문서 목록을 블루프린트에 고정. "이미 확정 — 변경 금지" | "1단계 생략, 디렉토리 생성부터" | 저자가 구조를 완전히 통제 |
| **구조 설계형** | 1단계에서 에이전트가 전체 구조를 먼저 설계·제안 | "1단계 구조 설계부터 시작" | 주제 탐색 여지가 클 때 |
| **Synthesis (횡단 비교)** | 챕터를 "기술별"이 아니라 **"질문별"** 로 구성, 매 챕터에서 N개 모델을 나란히 비교 | 설계형과 동일 + "항상 여러 모델을 나란히" | 선행 레포들을 입력으로 삼음 |

**Synthesis 추가 규칙**

1. **항상 나란히** — 한 모델만 설명하지 말고 최소 2~3개를 같은 표·코드로 비교한다.
2. **질문으로 환원** — 모든 기법을 "이건 어떤 공통 질문에 대한 답인가"로 분류한다.
3. **깊이는 선행 레포로 위임** — 각 기술의 *내부*는 해당 레포로 링크하고, 여기서는 *비교*에 집중한다.
4. **수렴과 발산** — "왜 모두 같은 방향으로 수렴했고, 어디서 갈렸는가"를 그린다.

---

## 4. 톤 & 표기

- **언어:** 본문은 한국어, 기술 고유명사·코드·수식은 원어 그대로. (예: "Operand Stack", `invokedynamic`, $\sqrt{d_k}$)
- **섹션 헤더:** `## 🎯 …` 형식의 **이모지 + 한국어**. 같은 의미의 섹션은 저장소 전체에서 같은 이모지를 씁니다.
- **호칭:** 독자에게 말 걸듯 반말 지시문 톤 ("…파헤치는 레포를 만들 거야", "시작해줘").
- **수식:** LaTeX (`$…$`, `$$…$$`). AI 레포는 수식이 1급 시민입니다.
- **강조:** 핵심 용어·대비 지점에 **굵게**. 남발 금지.

---

## 5. 생성 문서의 표준 구조 (10섹션)

블루프린트가 만들어내는 **모든 개별 문서**는 아래 두 표준 중 하나를 끝까지 따릅니다. 섹션을 건너뛰지 않습니다.

### 5.1 표준 v2 — 시스템 / Dev

```markdown
## 🎯 핵심 질문
## 🔍 왜 이 메커니즘이 필요한가 (없으면 어떤 문제가 생기는가)
## 😱 흔한 오해 또는 잘못된 사용 (Before)
## ✨ 올바른 이해와 사용 (After)
## 🔬 내부 동작 원리 (공식 스펙 / 1차 소스코드 추적)
## 💻 실험으로 확인하기 (실행 가능한 코드 + 도구 출력 분석)
## 📊 측정 결과 (벤치마크 / 프로파일 / 로그 실측)
## 🤔 트레이드오프
## 📌 핵심 정리
## 🤔 생각해볼 문제 (+ 해설)
```

### 5.2 수학 변형 — AI / 이론

```markdown
## 🎯 핵심 질문
## 🔍 왜 이 설계가 핵심인가
## 📐 수학적 선행 조건 (선행 레포 참조)
## 📖 직관적 이해 (그림 · 시각화)
## ✏️ 엄밀한 정의
## 🔬 정리와 증명
## 💻 구현 검증 (NumPy/PyTorch — 이론과 수치가 일치함을 보임)
## 🔗 실전 활용
## ⚖️ 가정과 한계
## 📌 핵심 정리
## 🤔 생각해볼 문제 (+ 해설)
```

### 5.3 공통 규칙
- **독립성:** 각 문서는 단독으로 읽혀야 합니다. 다른 문서의 사전 지식이 필요하면 *명시적 링크*를 답니다.
- **`🤔 생각해볼 문제`** 는 반드시 **해설을 동반**합니다. 던지고 끝내지 않습니다.
- **분량:** 문서당 권장 2,000~4,500단어 (코드·출력·실측 포함). 비교 레포는 2,000~3,000단어.

---

## 6. 실험 · 검증 환경

블루프린트는 "직접 돌려볼 수 있는" 환경을 명시합니다.

- **도구 목록을 박아둔다** — Dev는 `javap`, `JMH`, `JFR`, `async-profiler`, `EXPLAIN ANALYZE`, `wrk`, `strace` 등. AI는 `requirements.txt`(버전 고정)로.
- **기준 버전을 통일** — "HotSpot OpenJDK 21 기준", "PyTorch 2.1 기준"처럼 한 버전으로 못 박고, 다를 때만 차이를 표기합니다.
- **재현 가능성** — 측정값에는 환경(CPU/메모리/버전)을 함께 적습니다. 로그·Flame Graph는 직접 캡처해 첨부합니다.
- **다언어 비교(Synthesis)** 는 `docker-compose`/멀티스테이지로 polyglot 환경을 구성하고, *같은 과제*를 여러 언어로 구현해 나란히 측정합니다.

---

## 7. 증거와 출처

### 7.1 도구 출력
주장 옆에는 그것을 만든 명령과 출력을 같이 둡니다.

````markdown
```
$ javap -c -v Main.class
0: invokedynamic #2,  0
BootstrapMethods:
  0: REF_invokeStatic LambdaMetafactory.metafactory(...)
```
→ Lambda가 inner class가 아니라 invokedynamic을 쓰는 이유
````

### 7.2 코드 + 결과
실행 가능한 최소 예제와 그 **출력/그래프**를 함께 보입니다. 수학 레포는 "이론값 = 수치값" 일치를 코드로 증명합니다.

### 7.3 1차 출처만
`📚 참고 자료`는 **1차 출처**로 채웁니다 — 공식 스펙(JVMS/JLS, ECMA-262), 원전 논문(저자·연도 표기), 소스코드(OpenJDK, V8, Lucene). 블로그 요약본은 지양합니다.

---

## 8. 네이밍 & 파일 규약

- **블루프린트 파일명:** `prompt-{주제}-deep-dive.md` (소문자·하이픈). 비교 레포는 `prompt-{주제}-compared.md`.
- **생성 레포의 문서 파일명:** `NN-kebab-case.md` (예: `03-volatile-deep-dive.md`), 번호로 순서 고정.
- **디렉토리:** 섹션은 의미 단위의 kebab-case (`garbage-collection/`, `java-memory-model/`).
- **카탈로그 동기화:** 새 블루프린트를 추가하면 **반드시 `README.md`의 해당 표에 한 줄**(`블루프린트 | 컨셉`)을 더합니다.

---

## 9. 블루프린트 작성 체크리스트

- [ ] 제목이 `# {주제} Deep Dive 레포지토리 제작 프롬프트` 형식인가
- [ ] 첫 문장이 *"…하는 것과 …아는 것은 다르다"* (사용 ≠ 본질)인가
- [ ] `컨셉` · `핵심 차별화` · `타겟 독자`가 모두 있고, 독자가 구체적인가
- [ ] 선행 학습 / 레포 연결을 명시했는가 (§1.5)
- [ ] 전체 구조에 `핵심 질문`과 문서 표가 있고, **총 문서 수를 산식으로 확정**했는가
- [ ] 생성 문서의 10섹션 표준(v2 또는 수학 변형)을 코드블록으로 박았는가
- [ ] 모든 주장을 증거(도구 출력 / 코드 / 증명)로 받친다고 못 박았는가
- [ ] 기준 버전·실험 환경을 통일했는가
- [ ] `참고 자료`가 1차 출처인가
- [ ] `핵심 분석 대상`에 대표 쇼케이스(코드/수식/다이어그램)가 있는가
- [ ] `README.md` 카탈로그 표에 한 줄을 추가했는가
